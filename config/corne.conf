# Deep sleep to save battery
CONFIG_ZMK_SLEEP=y

# nice!view display
CONFIG_ZMK_DISPLAY=y

# Pointing device support (Mouse + wheel + button layers)
CONFIG_ZMK_POINTING=y

# External power rail control (used by the Media layer's EP_TOG key)
CONFIG_ZMK_EXT_POWER=y
